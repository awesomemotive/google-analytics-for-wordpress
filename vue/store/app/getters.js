const blocked = state => state.blocked;
const notices = state => state.notices;
const mi = state => state.mi;
const ftp_form = state => state.ftp_form;

export default {
	blocked,
	notices,
	mi,
	ftp_form,
};
