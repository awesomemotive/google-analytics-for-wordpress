const reports = state => state.reports;
const width = state => state.width;
const loaded = state => state.loaded;
const error = state => state.error;
const notice30day = state => state.notice30day;

export default {
	reports,
	width,
	loaded,
	error,
	notice30day,
};
