const date = state => state.date;
const pageinsights = state => state.pageinsights;
const loaded = state => state.loaded;
const error = state => state.error;
const noauth = state => state.noauth;

export default {
	date,
	pageinsights,
	loaded,
	error,
	noauth,
};
